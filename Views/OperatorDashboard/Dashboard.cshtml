<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Operator Dashboard - TravelSaaS</title>
    <link rel="stylesheet" href="~/css/site.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body {
            background: #f8f9fa;
            min-height: 100vh;
            display: block;
        }
        
        .dashboard-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
        }
        
        .dashboard-header {
            background: white;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            margin-bottom: 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .dashboard-header h1 {
            color: #333;
            margin: 0;
            font-size: 2.5rem;
            font-weight: 300;
        }
        
        .user-info {
            text-align: right;
        }
        
        .user-info p {
            margin: 5px 0;
            color: #666;
        }
        
        .logout-btn {
            background: #dc3545;
            color: white;
            border: none;
            padding: 8px 16px;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            margin-top: 10px;
        }
        
        .logout-btn:hover {
            background: #c82333;
            color: white;
            text-decoration: none;
        }
        
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }
        
        .stat-card {
            background: white;
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            text-align: center;
            transition: transform 0.3s ease;
        }
        
        .stat-card:hover {
            transform: translateY(-5px);
        }
        
        .stat-card h3 {
            color: #667eea;
            font-size: 2.5rem;
            margin-bottom: 10px;
            font-weight: 300;
        }
        
        .stat-card p {
            color: #666;
            font-size: 1rem;
            margin: 0;
        }
        
        .stat-card i {
            font-size: 2rem;
            color: #667eea;
            margin-bottom: 15px;
        }
        
        .actions-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
        }
        
        .action-card {
            background: white;
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }
        
        .action-card:hover {
            transform: translateY(-5px);
        }
        
        .action-card h3 {
            color: #333;
            margin-bottom: 15px;
            font-size: 1.3rem;
            font-weight: 500;
        }
        
        .action-card p {
            color: #666;
            margin-bottom: 20px;
            line-height: 1.5;
        }
        
        .action-btn {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            padding: 12px 20px;
            border-radius: 8px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        
        .action-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(102, 126, 234, 0.3);
            color: white;
            text-decoration: none;
        }
        
        .action-btn i {
            margin-right: 8px;
        }
    </style>
</head>

        <body>
    <div class="dashboard-container">
        <div class="dashboard-header">
            <h1><i class="bi bi-building"></i> Opérateur Client Dashboard</h1>
            <div class="user-info">
                <p><i class="bi bi-person-circle"></i> @User?.Identity?.Name</p>
                <p><i class="bi bi-shield"></i> Type de compte: Opérateur</p>
                <p><i class="bi bi-calendar"></i> @DateTime.Now.ToString("dd/MM/yyyy")</p>
                <a href="/Home/Index" class="logout-btn">
                    <i class="bi bi-box-arrow-right"></i> Déconnexion
                </a>
            </div>
        </div>

        <div class="stats-grid">
            <div class="stat-card">
                <i class="bi bi-ticket-perforated"></i>
                <h3 id="myReservations">-</h3>
                <p>Mes Réservations</p>
            </div>
            <div class="stat-card">
                <i class="bi bi-check-circle"></i>
                <h3 id="validatedReservations">-</h3>
                <p>Validées Aujourd'hui</p>
            </div>
            <div class="stat-card">
                <i class="bi bi-clock"></i>
                <h3 id="pendingReservations">-</h3>
                <p>En Attente</p>
            </div>
            <div class="stat-card">
                <i class="bi bi-star"></i>
                <h3 id="rating">-</h3>
                <p>Note Moyenne</p>
            </div>
        </div>

        <div class="actions-grid">
            <div class="action-card">
                <h3><i class="bi bi-list-check"></i> Valider les Réservations</h3>
                <p>Traiter et valider les réservations en attente de validation.</p>
                <button class="action-btn" onclick="validateReservations()">
                    <i class="bi bi-check-circle"></i> Valider
                </button>
            </div>
            
            <div class="action-card">
                <h3><i class="bi bi-search"></i> Rechercher des Réservations</h3>
                <p>Rechercher et consulter les réservations par critères.</p>
                <button class="action-btn" onclick="searchReservations()">
                    <i class="bi bi-search"></i> Rechercher
                </button>
            </div>
            
            <div class="action-card">
                <h3><i class="bi bi-printer"></i> Imprimer les Billets</h3>
                <p>Imprimer les billets validés pour les clients.</p>
                <button class="action-btn" onclick="printTickets()">
                    <i class="bi bi-printer"></i> Imprimer
                </button>
            </div>
            
            <div class="action-card">
                <h3><i class="bi bi-person-lines-fill"></i> Service Client</h3>
                <p>Gérer les demandes et questions des clients.</p>
                <button class="action-btn" onclick="customerService()">
                    <i class="bi bi-headset"></i> Service Client
                </button>
            </div>
        </div>
    </div>

    <script>
        // Charger les statistiques au chargement de la page
        document.addEventListener('DOMContentLoaded', function() {
            loadStats();
        });

        async function loadStats() {
            try {
                // Simuler des données pour l'instant
                document.getElementById('myReservations').textContent = '23';
                document.getElementById('validatedReservations').textContent = '8';
                document.getElementById('pendingReservations').textContent = '3';
                document.getElementById('rating').textContent = '4.8';
            } catch (error) {
                console.error('Erreur lors du chargement des statistiques:', error);
            }
        }

        function validateReservations() {
            alert('Fonctionnalité de validation des réservations en cours de développement');
        }

        function searchReservations() {
            alert('Fonctionnalité de recherche en cours de développement');
        }

        function printTickets() {
            alert('Fonctionnalité d\'impression en cours de développement');
        }

        function customerService() {
            alert('Fonctionnalité de service client en cours de développement');
        }
    </script>
</body>
</html>